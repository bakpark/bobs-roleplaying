{
    "name": "user_intent",
    "strict": true,
    "schema": {
        "type": "object",
        "properties": {
            "intent": {
                "type": "string",
                "description": "The intent of the user's last message.",
                "enum": [
                    "Answer",
                    "Stop the question",
                    "Acceptance [FINAL OUTPUT]"
                ]
            }
        },
        "required": [
            "intent"
        ],
        "additionalProperties": false
    }
}